[project]
name = "ucas-coures"
version = "0.1.0"
description = "UCAS 教务系统抢课脚本（Python + Selenium）"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "selenium>=4.0.0",
    "webdriver-manager>=4.0.0",
    "ddddocr>=1.4.0",
]

[tool.uv]
package = false
